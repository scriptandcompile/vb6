name: Update VB6Runtime Library Documentation

on:
  push:
    branches: [master]
    paths:
      - 'projects/vb6runtime/src/library/**'
      - 'projects/vb6runtime/scripts/generate-library-docs.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  library:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: pip install markdown

      - name: Generate library documentation
        run: |
          python3 projects/vb6runtime/scripts/generate-library-docs.py \
            --src projects/vb6runtime/src/library \
            --output docs/vb6runtime/library \
            --clean

      - name: Commit and push if changed
        run: |
          git pull --rebase --autostash
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/vb6runtime/library/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update library documentation"
            git push
          fi
